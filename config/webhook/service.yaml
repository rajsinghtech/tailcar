apiVersion: v1
kind: Service
metadata:
  name: tailcar-webhook
  namespace: tailcar-system
spec:
  ports:
  - name: webhook
    port: 443
    targetPort: 9443
    protocol: TCP
  selector:
    app: tailcar
